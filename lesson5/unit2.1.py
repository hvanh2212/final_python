import csv
import yaml
"""

"""
csv_data = []
with open('folder/file_json.csv') as csvfile:
    reader = csv.DictReader(csvfile)
    for row in reader:
        csv_data.append(row)
 
with open("folder/file_json" + '.yml', 'w') as outfile:
    outfile.write(yaml.dump(csv_data))

